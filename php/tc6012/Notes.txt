Steps to Add Virtual Host to Apache

1. Create new directory location structure
2. Add server name to HOSTS file (a local server uses local address 127.0.0.1)
3. Add virtual host and if necessary directory entries to Apache config files
	(httpd.conf or related files)
4. Ensure directory is readable